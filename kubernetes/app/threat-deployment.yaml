apiVersion: apps/v1
kind: Deployment
metadata:
  name: threat-service
  namespace: quantum-defense
  labels:
    app: threat-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: threat-service
  template:
    metadata:
      labels:
        app: threat-service
    spec:
      containers:
      - name: threat-service
        image: quantum-defense/threat-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 4003
        envFrom:
        - configMapRef:
            name: c2-config
        env:
        - name: PORT
          value: "4003"
        - name: DATABASE_URL
          value: "postgresql://postgres:postgrespassword@db-service.quantum-defense.svc.cluster.local:5432/qdefense?schema=public"
        - name: JWT_SECRET
          value: "c2-top-secret-signing-key"
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "250m"
            memory: "256Mi"
        livenessProbe:
          httpGet:
            path: /api/threats/health
            port: 4003
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /api/threats/health
            port: 4003
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: threat-service
  namespace: quantum-defense
  labels:
    app: threat-service
spec:
  ports:
  - port: 4003
    targetPort: 4003
    protocol: TCP
  selector:
    app: threat-service
