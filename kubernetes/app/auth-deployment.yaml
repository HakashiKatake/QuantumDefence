apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: quantum-defense
  labels:
    app: auth-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
      - name: auth-service
        image: quantum-defense/auth-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 4001
        envFrom:
        - configMapRef:
            name: c2-config
        env:
        - name: PORT
          value: "4001"
        - name: DATABASE_URL
          value: "postgresql://postgres:postgrespassword@db-service.quantum-defense.svc.cluster.local:5432/qdefense?schema=public"
        - name: JWT_SECRET
          value: "c2-top-secret-signing-key"
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "250m"
            memory: "256Mi"
        livenessProbe:
          httpGet:
            path: /api/auth/health
            port: 4001
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /api/auth/health
            port: 4001
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: quantum-defense
  labels:
    app: auth-service
spec:
  ports:
  - port: 4001
    targetPort: 4001
    protocol: TCP
  selector:
    app: auth-service
